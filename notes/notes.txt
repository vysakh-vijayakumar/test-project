Linux logging in
================
# ssh
ssh user@ip-address
ssh ipaddress
.ppk (key file to use)

Linux Commands
==============
ls
ls -l
rm
rm -rf (recursive, forceful)
grep
find -n "*.txt" /home/ubuntu/
top
htop

git
===
git clone https://github.com/vysakh-vijayakumar/test-project.git
git status
git add notes/notes.txt
git commit -m "Added AWS git notes"
git push

git pull
git branch
git branch "development"
git checkout "development"

AWS
===
#1 Start EC2 instance
  - choose only t2.micro (free). No additional EBS volume
#2 Copy public IPv4 address
#3 Create .pem file or use an existing pem key
#4 Use Putty
   - Use PuttyGen to create .ppk file from .pem file
# User : ubuntu
 
